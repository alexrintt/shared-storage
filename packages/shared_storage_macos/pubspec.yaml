name: shared_storage_macos
description: Flutter network service discovery plugin (macOS submodule - will be installed with the parent plugin).
version: 1.0.3
repository: https://github.com/sebastianhaberey/shared_storage/tree/main/shared_storage_macos
issue_tracker: https://github.com/sebastianhaberey/shared_storage/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      macos:
        pluginClass: NsdMacosPlugin
