name: shared_storage_ios
description: Flutter network service discovery plugin (iOS submodule - will be installed with the parent plugin).
version: 1.0.3
repository: https://github.com/sebastianhaberey/shared_storage/tree/main/shared_storage_ios
issue_tracker: https://github.com/sebastianhaberey/shared_storage/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: NsdIosPlugin
